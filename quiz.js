let index=0,score=0,active=[];const select=$('#quizCategory');select.innerHTML=['Semua topik',...new Set(QUIZZES.map(q=>q.category))].map(x=>`<option>${x}</option>`).join('');select.onchange=start;function start(){active=QUIZZES.filter(q=>select.value==='Semua topik'||q.category===select.value);index=0;score=0;render()}function render(){const box=$('#quizBox');if(index>=active.length){box.innerHTML=`<p class="eyebrow">SELESAI</p><h2>Skor anda: ${score} / ${active.length}</h2><button class="next-button" id="retry">Cuba lagi</button>`;$('#retry').onclick=start;return}const q=active[index];box.innerHTML=`<div class="quiz-progress">SOALAN ${index+1} DARIPADA ${active.length} · SKOR ${score}</div><p class="quiz-question">${q.question}</p><div class="answers">${q.options.map((o,i)=>`<button class="answer" data-answer="${i}">${String.fromCharCode(65+i)}. ${o}</button>`).join('')}</div>`;$$('[data-answer]',box).forEach(b=>b.onclick=()=>answer(+b.dataset.answer))}function answer(a){const q=active[index],ok=a===q.answer;$$('.answer').forEach(b=>{b.disabled=true;b.classList.toggle('correct',+b.dataset.answer===q.answer);b.classList.toggle('wrong',+b.dataset.answer===a&&!ok)});if(ok)score++;$('#quizBox').insertAdjacentHTML('beforeend',`<div class="quiz-feedback"><strong>${ok?'Betul':'Belum tepat'}.</strong> ${q.explanation}</div><button class="next-button" id="next">${index+1===active.length?'Lihat skor':'Soalan seterusnya'}</button>`);$('#next').onclick=()=>{index++;render()}}start();
