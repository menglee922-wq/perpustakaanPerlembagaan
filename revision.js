const choices=$('#revisionChoices');choices.innerHTML=TOPICS.map(t=>`<button data-revise="${t.id}">${t.article}</button>`).join('');$$('[data-revise]').forEach(b=>b.onclick=()=>{const t=TOPICS.find(x=>x.id===b.dataset.revise);$$('[data-revise]').forEach(x=>x.classList.toggle('active',x===b));$('#revisionCard').innerHTML=`<p class="eyebrow">${t.article.toUpperCase()} · ${t.category.toUpperCase()}</p><h2>${t.title}</h2><p>${t.core}</p><ul>${t.details.slice(0,3).map(x=>`<li>${x}</li>`).join('')}</ul><div class="tag-list">${t.keywords.map(k=>`<span class="tag">${k}</span>`).join('')}</div><a class="next-button" href="article.html?id=${t.id}">Baca topik penuh</a>`});
